export type TCustomer = {
  _id: string;
  email: string;
  firstName: string;
  lastName: string;
  contactNumber: string;
  address: string;
  role: string;
  createdAt:string
  [key: string]: any;
};


export type TCustomerOrder = {
  
}